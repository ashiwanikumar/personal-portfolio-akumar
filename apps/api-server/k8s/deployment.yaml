apiVersion: apps/v1
kind: Deployment
metadata:
  name: corevault-landing-api
  namespace: corevault-labs
  labels:
    app: corevault-landing-api
    app.kubernetes.io/name: corevault-landing-api
    app.kubernetes.io/part-of: corevault-labs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: corevault-landing-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: corevault-landing-api
        app.kubernetes.io/name: corevault-landing-api
        app.kubernetes.io/part-of: corevault-labs
    spec:
      containers:
      - name: corevault-landing-api
        image: 172.16.100.33:8083/corevault-landing-api:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3001
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "768Mi"
            cpu: "500m"
        env:
        - name: NODE_ENV
          value: "production"
        - name: API_PORT
          value: "3001"
        - name: ENABLE_CLUSTER
          value: "false"
        - name: SCHEDULER_ENABLED
          value: "true"
        envFrom:
        - secretRef:
            name: corevault-landing-api-secret
        livenessProbe:
          httpGet:
            path: /api/v1/healthCheck
            port: 3001
          initialDelaySeconds: 45
          periodSeconds: 20
          failureThreshold: 3
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/healthCheck
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
          timeoutSeconds: 10
      imagePullSecrets:
      - name: nexus-registry-secret
